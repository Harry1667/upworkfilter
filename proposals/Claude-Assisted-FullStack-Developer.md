# 提案 — Claude Assisted Full-Stack/Systems Developer (n8n, React, React Native, Node.js, ClickUp)

> 客戶:Expert 等級 · Hourly · >30hr/週 · >6 個月 · 花費 $10K+ · 評分高
> 技能 match:**極高**(Claude 全套 / n8n / React Native / Node / Next.js 全中)
> 整理日期:2026-05-27
> ⚠️ 申請門檻高:**21 Connects + 要錄 3 段影片 + 指定格式專案說明**。投入大,但 fit 完美、長期 → 值得認真投一次。

---

## 0. 兩個現實提醒(先看)

1. **地點偏好「印尼」= 軟性偏好,非硬性**。你照樣能投,只會被標一行「不符 Location 印尼」。客戶要印尼多半是「時區/成本」—— **台灣和印尼只差 1 小時,時區你完全沒問題**,在影片 Q3 主動講清楚(下方已寫進講稿)。
2. **報價建議 $20-22/hr**(維持你 profile rate)。這客戶 $10K+、要 Expert,不是壓價客;別自貶。**不要 bid 50 Connects 搶第一**,用 base 21,靠影片+專案品質取勝。

---

## 1. ✍️ Cover Letter(英文,可直接貼)

```
Your core challenge is moving fast across many projects — web, mobile, automations, internal tools — while keeping code quality and clean architecture. That's exactly how I already work: I build full-stack with React/React Native/Node/Next.js and use Claude Code daily for implementation, refactoring, and architecture decisions. I recently built AgentsHub, a real-time multi-user collaboration platform (Next.js + TypeScript + WebSocket), and shipped Claude-API features into production workflows. I'm also comfortable wiring n8n automations and operational integrations like ClickUp. Note: I'm in Taiwan — only ~1 hour from Indonesia time, so timezone overlap is full. I've attached my required project and video answers below. What's the first system you'd want me to take ownership of?
```

---

## 2. 📦 Required Project(指定格式,英文,可直接貼)

> 用 **AgentsHub** 當主打(最能秀 UI 複雜度 + Claude 工作流)。**括號內 [請確認] 的數字請改成你真實的**。

```
PROJECT: AgentsHub — Real-time AI-agent collaboration platform

Short description:
A multi-user workspace where teams and AI agents collaborate in real time — live chat, shared meetings, and a pixel-art virtual office. Built as a full-stack product, front to back.

What I personally built:
The entire stack — frontend UI/UX, real-time sync layer, backend APIs, and deployment. No team; I owned every layer.

Frontend / backend technologies:
Frontend: Next.js, React, TypeScript, Turbopack. Realtime: WebSocket-based presence & sync. Backend: Node.js APIs. Deployed on cloud infrastructure.

What makes it complex:
Real-time multi-user state (presence, live updates, conflict-free sync), a virtual-office spatial UI, and keeping it responsive under concurrent users — the hard parts are state consistency and latency, not just CRUD.

What makes the UI strong:
A clean, modern, game-like virtual office that's still intuitive — clear affordances, smooth transitions, and a layout that makes "who's here / what's happening" obvious at a glance.

Which Claude tools I used:
Claude Code for implementation/refactoring/debugging, and Claude for architecture and feature planning.

How I used Claude during the process:
I used Claude to reason through the real-time sync architecture and edge cases, scaffold components fast with Claude Code, then iterate. I treat it as a senior pair — I direct the design, it accelerates execution.

What was AI-assisted vs what I personally reviewed/changed:
AI-assisted: boilerplate, component scaffolding, first-pass refactors. I personally owned: the sync architecture decisions, performance tuning, and every UI/UX judgment call — I reviewed and rewrote anything that didn't meet the quality bar.

How long it took / with Claude Code:
[請確認:整體約 X 週;用 Claude Code 把實作時間壓縮約 Y%]

Final result:
A working real-time collaboration platform demonstrating production-level full-stack and AI-accelerated workflow.
```

> 替代主打:若你更想秀「Claude API 上線」就改用 **WorkLog**(Next.js + Claude API 智慧工作日誌);若想秀後端規模就用 **api-dindon**(FastAPI + Celery,29 家醫院)。但這案重 UI + Claude,**AgentsHub 最對口**。

---

## 3. 🎥 影片回答講稿(3 題 · 對著鏡頭講,英文)

> 每段 30-60 秒即可。下面是「可直接念」的英文 + 重點。自然、別背稿感。

**Video 1 — Tell me about yourself and your experience**
```
Hi, I'm Po-Han, a full-stack developer focused on AI-assisted product development. I build across the whole stack — React, React Native, Node, Next.js, and Python — from polished UIs to scalable backends and deployment. I work the way you described: Claude Code is part of my daily workflow for coding, refactoring, and architecture, so I move fast without dropping quality. Recent work includes AgentsHub, a real-time collaboration platform, and several Claude-API and automation projects. I like owning systems end to end and figuring out the gaps myself.
```

**Video 2 — How do you use Claude in your design process**
```
I use Claude as a thinking and acceleration partner, not autopilot. For design, I describe the user problem and constraints, ask Claude to propose layouts and flows, then I push on the UX judgment — what's actually clean and usable. For build, Claude Code scaffolds and refactors fast while I own architecture and review every output against a quality bar. So the loop is: I direct, Claude accelerates, I validate and refine. That keeps speed high and quality intact.
```

**Video 3 — Work hours, availability, how many hours**
```
I'm based in Taiwan, which is only about one hour from Indonesia time — so we'd have full working-hours overlap. I can commit 30-plus hours a week and I'm comfortable managing multiple active projects in parallel. I'm self-managed and responsive, so you won't need to micromanage — I take a requirement, find the gaps, and turn it into a working system.
```
> ⚠️ Q3 的工時/可投入時數請改成你**真實**能承諾的數字。

---

## 4. 其他欄位

| 欄位 | 內容 |
|------|------|
| Bid rate | **$20-22/hr**(不自貶,不衝榜) |
| Connects | base **21**,不要加碼搶第一 |
| Attachments | AgentsHub 截圖(虛擬辦公室 UI 最吸睛);次選 WorkLog 的 Claude 對話畫面 |
| Profile highlights(4) | Claude 全棧工作流 · 即時協作系統 · React/Next.js · n8n 自動化 |
| GitHub | https://github.com/Harry1667 |

---

## 5. 投不投?最終判斷

**值得投,但條件是「影片+專案要做到位」**。技能完美對口、長期、好客戶;新手身分+Expert要求+競爭是逆風,**靠這份高品質申請拉回來**。若你今天沒空好好錄影片 → 先存著,有空再投(案子可能還在)。
