# UI 重構規劃 — 導航分區 + 使用流程

> 狀態:**規劃中,待確認再實作**。最後更新 2026-05-27。
> 目標:把「功能導向的散頁」重組成「接案動線導向的分區」。

---

## 0. 一句話結論

導航從現在的 4 個平行功能頁(案子 / 客戶回覆 / 我的檔案 / 評分設定 + 孤兒 /analysis),
改成**對齊接案動線的 3 主區 + 1 工具區**:**探索 → 評估 → 溝通 ｜ 設定**。

---

## 1. 現況問題

| 問題 | 說明 |
|------|------|
| 頁面是「功能」不是「流程」 | 平行排 4 個,看不出先做哪個 |
| `/analysis` 是孤兒 | 評估網站沒掛導航,只能從列表點進,回不去也找不到 |
| 評估動作分散 | 評估網站、求職信、投標建議是 3 個分開的 API/位置,投一個案要跳好幾次 |
| 檔案 vs 設定混淆 | `/profile`(檔案+Agent)和 `/settings`(權重)都是「設定類」卻平排在主導航 |

---

## 2. 分區結論(4 區)

```
┌─ 主動線(接案三步)──────────────┐   ┌─ 工具 ─┐
│  ① 探索        ② 評估        ③ 溝通  │   │ ⚙ 設定 │
│  Discover      Evaluate      Engage  │   │ Setup  │
└──────────────────────────────────┘   └────────┘
```

| 區 | 路由 | 內容 | 對應現有 |
|----|------|------|---------|
| ① 探索 | `/` | 案件列表 + 篩選 + 今日新案動線 | 現 `/`(pageJobs) |
| ② 評估 | `/job?id=` | **單一案整合頁**:評估網站 + 求職信 + 投標建議 + 客戶背景,一頁搞定 | 現 `/analysis` + cover-letter/advice API 整合 |
| ③ 溝通 | `/reply` | 客戶回覆助手(投案後來回) | 現 `/reply` |
| ⚙ 設定 | `/setup` | 子頁籤:我的檔案 · Profile Agent · 評分權重 | 現 `/profile` + `/settings` 合併 |

---

## 3. 導航 Mockup

桌面頂部:
```
Upwork 接案助手          ① 探索   ② 評估   ③ 溝通          ⚙ 設定
                         APPLY 17 · MAYBE 3 · 共 20
```
- 主動線 3 區放中間(強調流程順序),設定縮到右邊(低頻、工具性)。
- 「評估」平常 disabled/灰,點進某個案才 active,並顯示目前在看哪個案。
- 列表頁保留現有的 APPLY/MAYBE/SKIP 篩選 chips。

---

## 4. 使用流程(重組後)

```
擴充套件(每15分)→ /api/ingest → 7維+作品契合評分 → 入庫
        │
        ▼
① 探索 /         今天有什麼 → 篩 APPLY → 看評分卡(含 ✓作品 證據)
        │  點一張卡
        ▼
② 評估 /job?id=  一頁看完:核心數據 / 客戶 / 競爭 / 開發流程
                  → 一鍵「產求職信」(引用真實 repo) + 「投標建議」(含建議附哪張截圖)
                  → 標記「已投」
        │  投出去、客戶回訊
        ▼
③ 溝通 /reply    貼客戶訊息 → AI 擬回覆 + 風險提醒
        │
        ▼
(學習迴路)標記 已投/已回/已錄取 → 之後餵回評分修正
```

設定區是隨時可去的旁路:調權重門檻、編輯檔案、手動跑 Profile Agent。

---

## 5. 介面/互動細節(這次會做的)

- **統一導航**:`navBar` 改成 3 主區 + 設定;active 樣式沿用現有。
- **評估整合頁 `/job?id=`**:把現在 `/analysis` 的評估網站,加上「求職信」「投標建議」兩個可展開區塊(呼叫既有 API),不再各自分散。標題列放「← 回列表」「🔗 Upwork 原案」「標記已投」。
- **列表動線提示**:頂部加「今日新案 N · 未處理 N」;卡片維持現有 7 維評分 + `✓作品` 證據。
- **設定分頁**:`/setup` 用頁籤切「檔案 / Agent / 權重」,把 `/profile`+`/settings` 收進來。
- 視覺沿用現有 dark CSS 變數(`--bg/--card/--ac` 等),不另起爐灶。

## 6. 不做(這次先不碰)

- 不改評分演算法、不改 ingest/DB schema。
- 不做 Proposal/Conversation/Coach Agent(見 AGENTS-PLAN 第 5 節)。
- 不動 SSL / 部署流程。

---

## 7. 分階段實作順序(分區域逐步進行)

> 你選「先出設計規劃」,以下是確認後的建議施工順序,每步可獨立上線驗證:

1. **Step 1 導航骨架**:`navBar` 改 3 主區 + 設定;新增 `/setup` 把 profile/settings 收進頁籤。風險低、先上。
2. **Step 2 評估整合頁**:`/analysis` → `/job?id=`,內嵌求職信 + 投標建議。最大價值。
3. **Step 3 列表動線**:今日新案/未處理提示 + 卡片小優化。
4. 每步:本機改 → push → server `git reset --hard` + `pm2 restart` → 線上驗證。

---

## 8. 待你拍板的點

- [ ] 路由命名:`/job` vs `/case` vs 沿用 `/analysis`?(我傾向 `/job?id=`)
- [ ] 設定頁路由:`/setup` 用頁籤,OK?還是保留 `/profile`、`/settings` 兩個各自獨立?
- [ ] 導航文字:要不要標數字「① 探索」,還是純文字「探索」?
